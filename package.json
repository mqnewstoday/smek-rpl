{
  "scripts": {
    "build:backend": "npm install --prefix apps/backend && npx prisma generate --schema=prisma/schema.prisma && npm run --prefix apps/backend build",
    "start:backend": "npm run --prefix apps/backend start:prod",
    "build:frontend": "npm install --prefix apps/frontend && npm run --prefix apps/frontend build",
    "start:frontend": "npx serve -s apps/frontend/dist/frontend/browser -l $PORT"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^26.0.1",
    "bcrypt": "^6.0.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
